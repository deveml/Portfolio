import { PaletteOptions } from "@mui/material";

/**
 * @description The color palette that's going to be used through out your application.
 * @note This can obviously be overridden at component level, but this gets applied to all the components under `<ThemeProvider>`
 */
export const colorPalette: PaletteOptions = {
  // Add your custom config here
};
