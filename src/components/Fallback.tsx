/**
 * @description A fallback component that gets loaded/displayed/rendered on the screen if the component doesn't get rendered on the DOM.
 * @note You can update the text and add your own content in the fallback component
 * @returns {React.ReactNode}
 */

export const FallBack = () => {
  return <>Uh-oh! We just ran into an error</>;
};
